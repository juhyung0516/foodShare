import React from "react";
import PaginationSSR from "components/pagination/paginationSSR";

const test = () => {
  return (
    <div>
      <PaginationSSR />
    </div>
  );
};

export default test;
